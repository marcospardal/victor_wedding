import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    background: {
      default: '#FAF3E9',
      paper: '#AEA38E'
    },
    primary: {
      main: '#53583E'
    },
    secondary: {
      main: '#FFD700'
    },
  }
});

export default theme;